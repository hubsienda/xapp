import type { Locale } from "./locales";

export const campaignExtraQuestions: Record<Locale, string[]> = {
  en: ["Are suppression lists and exclusions understood?"],
  es: ["¿Se entienden las listas de exclusión y supresión?"],
  it: ["Le liste di esclusione e soppressione sono chiare?"]
};
